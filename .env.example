# ============================================
# Configuração para Desenvolvimento Local
# ============================================
# Em produção, essas variáveis serão carregadas automaticamente
# do AWS Systems Manager Parameter Store via lib/aws/ssm-client.ts

# ============================================
# Banco de Dados PostgreSQL
# ============================================
# Formato: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:senha_segura_aqui@localhost:5432/pequenos_grupos
DATABASE_USER=postgres
DATABASE_PASSWORD=senha_segura_aqui
DATABASE_NAME=pequenos_grupos

# ============================================
# Aplicação
# ============================================
# Secret usado para JWT e outras operações criptográficas
# Gere uma string aleatória segura: openssl rand -base64 32
APP_SECRET=sua-chave-secreta-aqui-gerar-com-openssl-rand-base64-32
NODE_ENV=development

# ============================================
# AWS (Opcional para desenvolvimento local)
# ============================================
# Apenas necessário se quiser testar integração com SSM localmente
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=opcional-para-dev-local
# AWS_SECRET_ACCESS_KEY=opcional-para-dev-local

# ============================================
# Notas Importantes
# ============================================
# 1. Em produção na EC2, a aplicação busca automaticamente do SSM Parameter Store
# 2. Os parâmetros no SSM devem seguir o padrão: /pequenos-grupos/{categoria}/{nome}
# 3. Exemplo de parâmetros no SSM:
#    - /pequenos-grupos/database/url
#    - /pequenos-grupos/database/user
#    - /pequenos-grupos/database/password
#    - /pequenos-grupos/app/secret
#    - /pequenos-grupos/app/node-env
# 4. Veja DEPLOY_AWS_GUIDE.md para instruções completas de configuração

# ============================================
# Notificações por E-mail (AWS SES)
# ============================================
# Endereço remetente verificado no SES (sem isso, e-mails não são enviados)
# AWS_SES_FROM_EMAIL=noreply@seudominio.com

# ============================================
# Cron de Alertas
# ============================================
# Secret para proteger a rota GET /api/cron/check-alerts
# Gere com: openssl rand -hex 32
# CRON_SECRET=sua-chave-secreta-para-o-cron

# ============================================
# Produção EC2 (docker-compose.prod.yml)
# ============================================
# Necessário para rodar docker-compose manualmente na EC2
# DOCKER_IMAGE=ghcr.io/SEU_USER/pequenos-grupos:latest
# NEXT_PUBLIC_APP_URL=https://seu-dominio.com
# DATABASE_URL: o compose usa host "postgres" automaticamente (não localhost)
